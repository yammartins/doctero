{
  "name": "@core/styles",
  "main": "lib/index.ts",
  "author": "Yuri Martins",
  "version": "0.0.1",
  "private": false,
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "babel lib --out-dir ./dist -D --extensions '.ts,.tsx'",
    "watch": "babel lib --watch --out-dir ./dist -D --extensions '.ts,.tsx'"
  },
  "babel": {
    "presets": [
      "module:@core/babel"
    ]
  }
}
